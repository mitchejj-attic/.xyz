<!doctype html>
<html lang="en-US">
<head>
  {{> head }}
</head>
<body class="nullog">
 <header class='center'>
   <h2>nullog</h2>
  </header>
  <main class='nullog-body'>
    <article class="page {{~tag~}}">
      <header class="title">
        <h1 class="lowercase">{{ title }}
          {{~#if subtitle~}}<span class="mute"> {{ subtitle }}</span>{{~/if~}}
        </h1>
      </header>
      {{{ contents}}}
    </article>
</main>
  <header class='site-header center'>
    {{> nav-header }}
  </header>
<footer>
  {{> footer }}
</footer>
</body>
</html>
